# 이 파일은 커밋됨(키 목록 공유용). 실제 값은 .env에 — .env는 .gitignore로 커밋 금지.
# Supabase 대시보드 → Settings → API 에서 복사.
# ANON_KEY = Supabase 새 명칭의 "Publishable key"(sb_publishable_...)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
